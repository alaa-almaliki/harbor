# Harbor project manifest — committable single source of truth. Edit freely.
framework: {{FRAMEWORK}}
php: "{{PHP_VER}}"
# services: each entry is <name>: "<image:tag>". Bump a version by editing the
# value; use MariaDB via  mysql: "mariadb:11.4" ; add a service with a new line.
services: { {{SERVICES_MAP}} }
{{DB_BLOCK}}
# Optional: node: "20" · docroot: pub · domains: [x.test] · extensions: [imagick]
# php_ini: { memory_limit: 2G } · tools: [wkhtmltopdf] · multistore: { mode: domain, stores: {} }
# import: { strip_definers: true } · remote: { host: user@host, db: name, media: /path }
# remote:* fields may instead go in the gitignored .harbor/remote.env (keys
# HARBOR_REMOTE_HOST/_DB/_USER/_DB_HOST/_MEDIA[/_DB_PASSWORD]) to keep prod out of git.
